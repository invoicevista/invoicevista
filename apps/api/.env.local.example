# API Configuration
NODE_ENV=development
API_URL=http://localhost:3002

# Database Configuration (when needed)
# DATABASE_URL=postgresql://user:password@localhost:5432/invoicevista

# Redis Configuration (for rate limiting - when needed)
# REDIS_URL=redis://localhost:6379

# Email Service (when needed)
# EMAIL_FROM=noreply@invoicevista.com
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-smtp-user
# SMTP_PASS=your-smtp-password